import Swiper from "swiper";
import 'jquery-zoom';
import {$document} from "../constants";
/*
$(window).resize(function() {
    if($document.width() < 500) {

        //swiperMainTabs.destroy();
    } else {
        initMainTabsSlider();
    }

});


function initMainTabsSlider() {
    const swiperMainTabs = new Swiper(".tabs-box", {
        loop: false,
        slidesPerView: 'auto',
    });
}

initMainTabsSlider();
*/






